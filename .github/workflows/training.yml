name: Training
on:
  pull_request:
    branches: [ training ]
  workflow_dispatch:
    inputs:
      ping:
        description: 'This ping input would be returned in the output'
        required: true
        default: 'Hello'
jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - run: echo ${{github.event.inputs.ping}}